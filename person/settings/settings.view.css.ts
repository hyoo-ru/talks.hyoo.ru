namespace $.$$ {
	
	const { rem } = $mol_style_unit
	
	$mol_style_define( $hyoo_talks_person_settings, {
		
		flex: {
			basis: rem(26),
			shrink: 0,
		},
		
		Form: {
			padding: $mol_gap.block,
		},
		
	} )
	
}
